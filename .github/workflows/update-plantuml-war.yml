name: Safe Update PlantUML Server WAR (GitHub Status) for PUML-Server-Deployer branch
on: workflow_dispatch
jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Placeholder to enable UI button"
